import React from "react";
export const metadata = {
  title: "Leave your comment | GF",
  description: "Giuseppe Ferlazzo",
};

const layout = ({ children }: { children: React.ReactNode }) => {
  return <>{children}</>;
};
export default layout;
