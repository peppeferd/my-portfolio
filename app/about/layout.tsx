import React from 'react'
export const metadata = {
  title: 'Giuseppe Ferlazzo | About',
  description: 'Giuseppe Ferlazzo | About',
}

const layout = ({ children }: { children: React.ReactNode }) => {
  return <>{children}</>
}
export default layout
