import React from 'react'
export const metadata = {
  title: 'Giuseppe Ferlazzo | Projects',
  description: 'Giuseppe Ferlazzo | Projects',
}

const layout = ({ children }: { children: React.ReactNode }) => {
  return <>{children}</>
}
export default layout
